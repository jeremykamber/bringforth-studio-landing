# Bringforth Studio — Landing

Bringforth Studio is an AI Product Lab that houses AI-centric apps and startup ideas. This repo contains the Next.js landing page and a small design system.

Local dev (after installing dependencies):

- npm install
- npm run dev

Design system: Tailwind CSS + tokens. UI components use shadcn/ui patterns (manual integration). See /components and /design for tokens and components.
